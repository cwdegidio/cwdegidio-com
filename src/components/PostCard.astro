---
const { title, pubDate, description, url} = Astro.props;
---
<div class="post-card">
    <h2>{title}</h2>
    <p class="post-card-date">{pubDate}</p>
    <p>{description}</p>
    <p><a href={url} aria-label={`Read more about ${title}`}>Read more</a></p>
</div>
<style>
    .post-card {
        background-color: rgba(0, 0, 0, 0.25);
        padding: 1rem;
        margin-bottom: 1.5rem;
        border-radius: 10px;
        box-shadow: 0 2px 8px hsl(240 3.8% 46.1% / 10%);
    }

    .post-card > h2 {
        color: hsl(178, 100%, 40%);
        margin-top: 0;
    }

    .post-card > .post-card-date {
        font-size: 0.85rem;
        font-style: italic;
        margin-top: -10px;
    }
</style>